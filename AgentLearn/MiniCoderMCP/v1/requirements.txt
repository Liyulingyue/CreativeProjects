# MiniCoder 依赖
openai
python-dotenv
mcp   # MCP Python SDK for tool/server communication
fastapi
pydantic
requests  # 用于 MCPHttpClient 的 HTTP 通信
uvicorn[standard]  # 用于启动 FastAPI 服务器
# 可选依赖
# gradio>=4.0.0  # 如果需要Web界面
# rich>=13.0.0   # 如果需要美化输出
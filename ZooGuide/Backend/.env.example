# LLM 配置 (OpenAI 兼容)
OPENAI_API_KEY=sk-xxx
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Backend 服务
HOST=0.0.0.0
PORT=8000

# 是否启用 LLM（关闭时使用规则引擎回退，保证 demo 仍可运行）
USE_LLM=true

# CORS 允许的前端来源（逗号分隔）
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
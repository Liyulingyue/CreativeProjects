[project]
name = "openaijsonwrapper"
version = "0.2.0"
description = "小型 JSON 封装，适配 OpenAI 风格的数据与自定义类型序列化，支持多模态图片输入"
authors = [ { name = "Liyulingyue" } ]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=42","wheel"]
build-backend = "setuptools.build_meta"

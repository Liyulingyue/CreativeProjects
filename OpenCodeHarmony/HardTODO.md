1. 页面顶端
    当软键盘弹出时，页面顶端会被推出屏幕，导致用户无法看到TitleBar。
2. 状态更新
    新建后端、会话时，返回到会话List和后端List页面时，页面不会刷新，导致用户无法看到新建的会话和后端。
3. 新建会话的文件夹选择器失效
    新建会话时，系统需要根据用户输入的文件夹路径，展示该路径下的文件夹列表供用户选择，但目前该功能无法正常工作，用户无法选择 / 之外的文件夹，只能手动输入。
4.  